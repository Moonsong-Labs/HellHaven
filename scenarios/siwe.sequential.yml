config:
  target: "http://localhost"
  processor: "../dist/src/processors/index.js"
  variables:
    ACCOUNT_MODE: sequential
    ACCOUNT_INDEX_START: 0
    ACCOUNT_INDEX_COUNT: 10
  phases:
    - name: sequential-range
      duration: 10
      arrivalRate: 1

scenarios:
  - name: siwe_sequential_derive_and_print
    flow:
      - function: deriveAndPrint


