config:
  target: "http://localhost"
  processor: "../dist/src/processors/index.js"
  variables:
    ACCOUNT_MODE: random
    ACCOUNT_INDEX_START: 0
    ACCOUNT_INDEX_COUNT: 10
    ACCOUNT_RANDOM_SEED: 12345
  phases:
    - name: random-range
      duration: 10
      arrivalRate: 1

scenarios:
  - name: siwe_random_derive_and_print
    flow:
      - function: deriveAndPrint


